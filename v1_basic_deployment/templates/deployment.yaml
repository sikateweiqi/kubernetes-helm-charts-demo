apiVersion: apps/v1
kind: Deployment
metadata:
  name: ngnix # label that it select
spec:
  replica: 3
  template:
    metadata:
      labels:
        name: nginx
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80
